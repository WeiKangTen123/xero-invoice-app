node_modules/
.env
*.log
logs/*.log
coverage/
dist/
.DS_Store
Thumbs.db
.vscode/
*.local

# Personal deployment scratch notes — not for the public repo (real server/GitHub access info)
documentation/important/

# Runtime data — contains live secrets (Xero/IMAP/API keys), user accounts, and real invoice PDFs
main/data/
PDF/
ui/node_modules/
ui/dist/

# Personal credential scratch notes — contain live API keys/secrets in plaintext
unused/notes/*.txt
unused/notes/*.bak
