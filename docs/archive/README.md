# Archive

Documents kept for history only. Nothing here describes the system as it runs.

- `postgres-era/` — the original setup guides and server specs, written when the
  app targeted PostgreSQL + Redis on Railway. The app has run on SQLite under
  pm2 on a single VM since July 2026; see the top-level README and
  `docs/RUNBOOK.md` for the current deploy, backup and restore procedures.
